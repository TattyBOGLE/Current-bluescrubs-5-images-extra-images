**Paste this into Replit AI:**

```
Three UI fixes to the Revision Panel and Reference Material sections 
in plab1-new.tsx (or RevisionPanel.tsx / ReferenceMaterialPanel.tsx 
if already extracted).

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FIX 1 — MOVE VISUAL SUMMARY PDF TO REVISION PANEL
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Currently the "Visual Summary PDF" button sits inside the Reference 
Material panel card. Move it into the Revision Panel, directly below 
the "WHAT TO READ" link and its navigate/description text.

Place it as a small inline link, not a yellow outlined button:
  📄 Visual Summary PDF  (opens in new tab, same URL as before)

Style: plain text link, same blue as the NICE hyperlink above it, 
with a small external-link icon after it. No border, no background.

Remove the "Visual Summary PDF" button from the Reference Material 
panel card entirely. The card should then just show:
  - PRIMARY / NICE Guideline badges
  - The hyperlink
  - The italic description text below it

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FIX 2 — MAKE THE FULL HYPERLINK TEXT CLICKABLE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Currently the links render as:
  [NICE — Hypertension Management] [small external link icon box]

The icon is a separate clickable element next to the text. 

Change this so:
  - The entire line "NICE — Hypertension Management" is the 
    clickable anchor tag — wrapping both the text AND the icon
  - The external link icon sits inline at the end of the text, 
    inside the same <a> tag, not as a sibling element
  - Clicking anywhere on the text or icon opens the URL

Apply this to every hyperlink in both the Revision Panel and the 
Reference Material panel.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FIX 3 — REMOVE THE SMALL LINK BOX ICON NEXT TO HYPERLINKS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

The external link icon currently renders as a small outlined square 
box (⧉ or similar) sitting visually separate from the link text, 
making it look like two separate clickable elements.

Replace it with a clean inline ExternalLink icon from lucide-react 
(already imported in the file) at 14px, vertically aligned to the 
text baseline, inside the anchor tag:

  import { ExternalLink } from 'lucide-react';

  <a href={url} target="_blank" rel="noopener noreferrer" 
     className="inline-flex items-center gap-1 text-blue-600 
                hover:underline">
    {label}
    <ExternalLink size={14} />
  </a>

Apply this pattern to every external link in both panels. Remove 
whatever element was previously rendering the separate icon box.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DO NOT CHANGE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- The URLs themselves — keep every href exactly as it is
- The orange Revision Panel header and teal Reference Material 
  header styling
- The COMMON MISTAKE section content and styling
- The PRIMARY / NICE Guideline badge styling
- The collapse/expand (chevron) behaviour on both panels
- Any other panels below Reference Material
```