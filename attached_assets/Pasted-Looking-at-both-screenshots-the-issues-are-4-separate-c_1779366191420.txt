Looking at both screenshots, the issues are: 4 separate coloured blocks taking up a lot of vertical space, repetitive button layouts, and Block 1 and Block 4 are essentially the same thing (fixed question counts) just separated. Here's the prompt:

---

**Paste this into Replit AI:**

```
The session setup screen in plab1-new.tsx (or SessionSetup.tsx if already 
extracted) currently shows 4 separate coloured block sections stacked 
vertically — Block 1 (Fixed), Block 2 (Timed), Block 3 (Unlimited), 
Block 4 (PLAB Simulation). It is cluttered and repetitive. Redesign it 
as a clean, compact single-page setup panel.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
NEW LAYOUT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Replace the 4 blocks with a 2-step selector:

STEP 1 — Mode (4 pill tabs in a single row):
  [ Practice ]  [ Timed ]  [ Unlimited ]  [ PLAB Exam ]

  - Single row of tab pills, no coloured backgrounds on the 
    whole section — just the selected tab gets a filled style
  - One line of description text below the tabs that updates 
    based on the selected mode:
      Practice   → "Complete a set number of questions at your own pace"
      Timed      → "Answer as many questions as possible within a time limit"
      Unlimited  → "Study without time pressure — continue as long as you want"
      PLAB Exam  → "Real exam conditions — exactly 1 minute per question"

STEP 2 — Count / Duration (shown below the tabs, updates per mode):

  Practice mode — show a single row of compact pill buttons:
    10  |  20  |  50  |  100  |  180 (PLAB Mock)

  Timed mode — show a single row of compact pill buttons:
    10m  |  30m  |  60m  |  2h  |  3h

  Unlimited mode — no step 2 needed, just show the 
    "Start Unlimited Practice" button immediately

  PLAB Exam mode — show a single row of compact pill buttons:
    10  |  20  |  50  |  60  |  180 (Full PLAB)
    With a small note: "1 minute per question"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STYLING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- Remove all the coloured section backgrounds (blue, orange, 
  green, purple) — use a single neutral card for the whole panel
- Tab pills: unselected = outline style, selected = filled blue
- Count/duration pills: same pattern — outline unselected, 
  filled blue when selected
- No bold coloured buttons taking up full row width — 
  pills should be compact, roughly 80–100px wide
- Keep the existing category and difficulty selectors 
  above this section exactly as they are
- The whole setup panel should fit comfortably on one screen 
  without scrolling on desktop

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
BEHAVIOUR
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- Selecting a mode tab resets the count/duration selection 
  to the first option in that mode automatically
- The "Start Session" / "Start Unlimited Practice" button 
  stays at the bottom of the panel, disabled until both 
  a mode and count are selected (except Unlimited which 
  only needs mode)
- All existing session start logic, state variables 
  (blockType, isTimedSession, sessionTimeLimit) and the 
  functions they feed into must continue to work exactly 
  as before — this is a UI-only change
- Map the new UI selections to the existing state:
    Practice  → blockType = 'block1', isTimedSession = false
    Timed     → blockType = 'block2', isTimedSession = true, 
                sessionTimeLimit = selected minutes
    Unlimited → blockType = 'block3', isTimedSession = false
    PLAB Exam → blockType = 'block1', isTimedSession = true, 
                sessionTimeLimit = questionCount * 1 minute

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DO NOT CHANGE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- Category selector (cardiovascular, respiratory etc.)
- Difficulty selector (easy, medium, hard)
- Any logic that runs after the session starts
- The navigation bar at the bottom of the quiz
```